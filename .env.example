# ========================================
# SellFast.Now - Environment Variables
# ========================================
# Copy this file to .env for local development
# For Railway deployment, set these in the Railway dashboard

# ========================================
# REQUIRED - Core Configuration
# ========================================

# Database - PostgreSQL Connection String
# Railway auto-provides this when you add PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/database

# Session Secret - Optional with Clerk (Clerk handles session management)
# Only needed if using custom server-side sessions
# SESSION_SECRET=your-secure-random-session-secret-minimum-32-characters

# Server Configuration
NODE_ENV=production
PORT=5000

# ========================================
# REQUIRED - Authentication (Clerk)
# ========================================
# Clerk authentication for user sign-in/sign-up
# Get keys from: https://dashboard.clerk.com

# Clerk Secret Key (backend - keep private)
CLERK_SECRET_KEY=sk_live_...

# Clerk Publishable Key (frontend - can be public)
# Note: Vite requires VITE_ prefix for client-side env vars
VITE_CLERK_PUBLISHABLE_KEY=pk_live_...

# ========================================
# REQUIRED - Object Storage
# ========================================
# NOTE: Current implementation uses Replit Object Storage
# For Railway, configure one of these options:

# Option 1: Google Cloud Storage (Direct)
# GCS_PROJECT_ID=your-gcp-project-id
# GCS_BUCKET_NAME=your-bucket-name
# GCS_CREDENTIALS_JSON={"type":"service_account",...}

# Option 2: AWS S3
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# Option 3: Cloudinary (simplest for images)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# Current Replit-specific variables (need migration):
PUBLIC_OBJECT_SEARCH_PATHS=public/images,public/avatars
PRIVATE_OBJECT_DIR=.private
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# ========================================
# OPTIONAL - AI Features
# ========================================
# OpenAI for intelligent listing creation coaching
# Falls back to mock data if not provided
OPENAI_API_KEY=sk-...

# ========================================
# OPTIONAL - Payments (Stripe)
# ========================================
# Only needed if enabling payment features

# Production Keys
STRIPE_SECRET_KEY=sk_live_...
VITE_STRIPE_PUBLIC_KEY=pk_live_...

# Test Keys (for development)
TESTING_STRIPE_SECRET_KEY=sk_test_...
TESTING_VITE_STRIPE_PUBLIC_KEY=pk_test_...

# ========================================
# OPTIONAL - Email Service (Resend)
# ========================================
# For sending transaction confirmation emails, reviews, etc.

# Direct Resend API Key (for Railway)
RESEND_API_KEY=re_...

# Current Replit Connector variables (need migration):
# REPLIT_CONNECTORS_HOSTNAME=...
# REPL_IDENTITY=...
# WEB_REPL_RENEWAL=...

# ========================================
# OPTIONAL - PostgreSQL Session Details
# ========================================
# Auto-provided by Railway when using PostgreSQL
# Used for session storage in connect-pg-simple

PGHOST=your-pg-host
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your-password
PGDATABASE=railway

# ========================================
# Development Only
# ========================================
# These are only used in local development

# Frontend Dev Server Port (Vite)
# VITE_PORT=5173

# Enable debug logging
# DEBUG=express:*
